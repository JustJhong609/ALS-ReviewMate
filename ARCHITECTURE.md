cd /workspaces/ALS-ReviewMate
npm install# ALS ReviewMate - System Architecture

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         Frontend                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Ionic React + TypeScript                  │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐ │  │
│  │  │   Learner   │  │   Teacher    │  │     Auth     │ │  │
│  │  │  Interface  │  │  Dashboard   │  │    System    │ │  │
│  │  └─────────────┘  └──────────────┘  └──────────────┘ │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Supabase Client SDK
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                      Supabase Backend                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                  PostgreSQL Database                  │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌─────────────┐  │  │
│  │  │profiles│ │subjects│ │quizzes │ │quiz_attempts│  │  │
│  │  └────────┘ └────────┘ └────────┘ └─────────────┘  │  │
│  │  ┌────────┐ ┌─────────┐ ┌──────────┐               │  │
│  │  │ topics │ │questions│ │ progress │               │  │
│  │  └────────┘ └─────────┘ └──────────┘               │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Supabase Authentication                  │  │
│  │         (Email/Password + Role Management)            │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Row Level Security (RLS) Policies           │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

### Learner Flow
```
User Login
    │
    ▼
Dashboard (Select Subject)
    │
    ▼
Subject View (Browse Topics/Quizzes)
    │
    ├─► Read Topics ──► Study Materials
    │
    └─► Take Quiz
         │
         ▼
     Quiz Interface
         │
         ├─► Answer Question
         │       │
         │       ▼
         │   Get Feedback
         │       │
         │       ▼
         │   Next Question
         │       │
         │       ▼
         │   (Repeat)
         │
         ▼
    Complete Quiz
         │
         ├─► Save to quiz_attempts
         │
         └─► Update progress
              │
              ▼
         View Results
              │
              ▼
         Progress Dashboard
```

### Teacher Flow
```
Teacher Login
    │
    ▼
Teacher Dashboard
    │
    ├─► Overview Tab
    │    │
    │    ├─► Total Students
    │    ├─► Total Quizzes
    │    ├─► Class Average
    │    └─► Students Needing Attention
    │
    ├─► Students Tab
    │    │
    │    └─► Individual Performance
    │         ├─► Average Score
    │         ├─► Total Attempts
    │         └─► Weak Subjects
    │
    └─► Subjects Tab
         │
         └─► Subject Analytics
              ├─► Total Attempts
              ├─► Average Score
              └─► Student Count
```

## 🗄️ Database Schema

```
┌──────────────┐
│   profiles   │ (extends auth.users)
├──────────────┤
│ id (PK)      │
│ email        │
│ role         │ ──► 'learner' or 'teacher'
│ full_name    │
│ created_at   │
└──────┬───────┘
       │
       │ Creates
       │
       ▼
┌──────────────────┐        ┌──────────────┐
│  quiz_attempts   │────────│   quizzes    │
├──────────────────┤        ├──────────────┤
│ id (PK)          │        │ id (PK)      │
│ user_id (FK)     │        │ subject_id   │◄────┐
│ quiz_id (FK)     │◄───────│ title        │     │
│ score            │        │ difficulty   │     │
│ percentage       │        │ passing_score│     │
│ answers (JSON)   │        └──────┬───────┘     │
│ completed_at     │               │             │
└──────────────────┘               │ Has         │
                                   │             │
                                   ▼             │
       ┌───────────────┐    ┌──────────────┐    │
       │   progress    │    │  questions   │    │
       ├───────────────┤    ├──────────────┤    │
       │ id (PK)       │    │ id (PK)      │    │
       │ user_id (FK)  │    │ quiz_id (FK) │    │
       │ subject_id(FK)│    │ question_text│    │
       │ mastery_level │    │ options(JSON)│    │ Belongs to
       │ last_accessed │    │ correct_ans  │    │
       └───────────────┘    │ explanation  │    │
                            └──────────────┘    │
                                                │
┌──────────────┐        ┌──────────────┐       │
│   subjects   │────────│    topics    │       │
├──────────────┤        ├──────────────┤       │
│ id (PK)      │        │ id (PK)      │       │
│ name         │        │ subject_id   │───────┘
│ description  │        │ title        │
│ icon         │        │ content      │
└──────────────┘        │ order_number │
                        └──────────────┘
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────┐
│          Row Level Security (RLS)            │
├─────────────────────────────────────────────┤
│                                              │
│  profiles Table:                             │
│  ✓ Users see own profile                    │
│  ✓ Users update own profile                 │
│                                              │
│  quiz_attempts Table:                        │
│  ✓ Users see own attempts                   │
│  ✓ Teachers see all attempts                │
│  ✓ Users create own attempts                │
│                                              │
│  progress Table:                             │
│  ✓ Users see own progress                   │
│  ✓ Teachers see all progress                │
│  ✓ Users update own progress                │
│                                              │
│  subjects, topics, quizzes, questions:       │
│  ✓ All authenticated users can read         │
│  ✓ Only teachers can create/update/delete   │
│                                              │
└─────────────────────────────────────────────┘
```

## 📱 Component Hierarchy

```
App
├── AuthProvider (Context)
│   └── AppRoutes
│       ├── Login
│       ├── Register
│       │
│       ├── Learner Routes
│       │   ├── LearnerDashboard
│       │   │   └── SubjectCard (x5)
│       │   ├── SubjectView
│       │   │   ├── TopicList
│       │   │   └── QuizList
│       │   ├── QuizPage
│       │   │   ├── ProgressBar
│       │   │   ├── QuestionCard
│       │   │   ├── FeedbackCard
│       │   │   └── CompletionCard
│       │   └── Progress
│       │       ├── SubjectProgress
│       │       └── RecentAttempts
│       │
│       └── Teacher Routes
│           └── TeacherDashboard
│               ├── OverviewTab
│               │   ├── StatsCards (x3)
│               │   └── AttentionList
│               ├── StudentsTab
│               │   └── StudentList
│               └── SubjectsTab
│                   └── SubjectAnalytics
```

## 🌐 Deployment Architecture

```
┌─────────────────────────────────────────────────────┐
│                   Web Deployment                     │
│  ┌───────────────────────────────────────────────┐  │
│  │  Vercel / Netlify                             │  │
│  │  ├─► Static Site Hosting                      │  │
│  │  ├─► Automatic Deployments                    │  │
│  │  ├─► CDN Distribution                         │  │
│  │  └─► HTTPS Certificate                        │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                         │
                         │ API Calls
                         ▼
┌─────────────────────────────────────────────────────┐
│                Supabase (Cloud)                      │
│  ├─► Database (PostgreSQL)                          │
│  ├─► Authentication Service                         │
│  ├─► Real-time Subscriptions                       │
│  └─► Storage (for future media)                    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                 Mobile Deployment                    │
│  ┌───────────────────────────────────────────────┐  │
│  │  Android (via Capacitor)                      │  │
│  │  ├─► Native App Container                     │  │
│  │  ├─► WebView with Ionic UI                    │  │
│  │  ├─► Native Plugins Access                    │  │
│  │  └─► Google Play Store Distribution           │  │
│  └───────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────┐  │
│  │  iOS (Future)                                 │  │
│  │  ├─► Native App Container                     │  │
│  │  ├─► WebView with Ionic UI                    │  │
│  │  └─► App Store Distribution                   │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

## 🔄 State Management

```
┌────────────────────────────────────────┐
│         React Context API              │
├────────────────────────────────────────┤
│                                         │
│  AuthContext                           │
│  ├─► user (User | null)                │
│  ├─► loading (boolean)                 │
│  ├─► signUp()                          │
│  ├─► signIn()                          │
│  └─► signOut()                         │
│                                         │
│  Component State (useState)            │
│  ├─► subjects                          │
│  ├─► quizzes                           │
│  ├─► questions                         │
│  ├─► progress                          │
│  └─► analytics                         │
│                                         │
└────────────────────────────────────────┘
```

## 📊 Performance Optimization

```
Frontend Optimization
├─► Code Splitting (React.lazy)
├─► Image Optimization
├─► Lazy Loading
├─► Memoization (useMemo, useCallback)
└─► PWA Caching

Backend Optimization
├─► Database Indexing
├─► Query Optimization
├─► RLS Policy Efficiency
└─► Connection Pooling

Build Optimization
├─► Vite Fast Builds
├─► Tree Shaking
├─► Minification
└─► Compression
```

---

This architecture is designed to be:
- ✅ Scalable
- ✅ Secure
- ✅ Maintainable
- ✅ Cost-effective
- ✅ User-friendly

**Last Updated:** December 4, 2025